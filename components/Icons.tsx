import React from 'react';
import { Car, Wallet, Wrench, Clock, Phone, Lock, Check, Star, Menu, X, MapPin, ChevronDown, ChevronUp } from 'lucide-react';

export const Icons = {
  Car,
  Wallet,
  Wrench,
  Clock,
  Phone,
  Lock,
  Check,
  Star,
  Menu,
  X,
  MapPin,
  ChevronDown,
  ChevronUp
};

export type IconName = keyof typeof Icons;